import { CollaboratorItemWrapper } from "./styles";

export const CollaboratorItem = ({text}) => {
    return (
        <CollaboratorItemWrapper>
            {text}
        </CollaboratorItemWrapper>
    );
};
